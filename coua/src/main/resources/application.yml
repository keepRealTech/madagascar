spring:
  application:
    name: coua
  datasource:
    url: jdbc:mysql://root:pass@localhost:3306/coua?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=CONVERT_TO_NULL&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
rocketmq:
  accessKey: xxx
  secretKey: xxx
  topic: notification-event
  tag: event
  nameSrvAddr: 127.0.0.1:9876

grpc:
  fossa:
    host: 127.0.0.1
    port: 12003
  port: 12002

snowflake:
  nodeId: 0

opentracing:
  jaeger:
    grpc-server-interceptor: false
    const-sampler:
      decision: false
